# Скопируй в .env и поправь под себя: cp .env.example .env
# Сервер не стартует, если что-то из обязательного не задано или задано неверно.

# NODE_ENV здесь намеренно не задаётся: сервер сам считает окружение разработческим,
# а сборке клиента эта переменная из общего .env ломала бы режим — Vite подхватил бы
# отсюда `development` и положил в бандл отладочную сборку React.
HOST=0.0.0.0
PORT=3000

# Строка подключения к Postgres. Значения совпадают с docker-compose.yml.
DATABASE_URL=postgres://gavan:gavan@localhost:5432/gavan

# Адрес самого сервера: он попадает в ссылку из письма для входа.
PUBLIC_URL=http://localhost:3000

# Чем подписывается сессия. В разработке можно не задавать; в бою сервер без него не стартует.
# Сгенерировать: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
# SESSION_SECRET=

# Origin клиента — нужен для CORS и для cookie с сессией.
WEB_ORIGIN=http://localhost:5173

# Адрес сервера для клиента. Переменные с префиксом VITE_ попадают в бандл,
# секретов здесь быть не может.
VITE_SERVER_URL=http://localhost:3000
